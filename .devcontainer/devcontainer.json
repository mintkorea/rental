{
    "name": "Sungeui Project",
    "build": { "dockerfile": "Dockerfile" },
    "remoteEnv": {
        "GOOGLE_CREDENTIALS": "${localEnv:GOOGLE_CREDENTIALS}" 
    },
    "customizations": {
        "vscode": {
            "extensions": ["ms-python.python", "googlecloudtools.cloudcode"]
        }
    }
}
